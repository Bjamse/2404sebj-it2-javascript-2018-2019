class playerClass {
    constructor(isPlayerOne){
        this.pieces= [];
        if (isPlayerOne)this.color = "white";
        else this.color = "black";


    }

    placePieces(numberOfPieces, borad){
        for (let i = 0; i < numberOfPieces; i++ ){
            board.
        }
    }

}